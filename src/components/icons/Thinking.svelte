<div class="thinking-icon">💭</div>

<style>
  .thinking-icon {
    text-align: center;
    overflow: visible;
    font-size: 20px;
    animation: bounce infinite alternate 1s;
    position: absolute;
    left: 64%;
    top: 2px;
  }
  @keyframes bounce {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(1.5);
    }
  }
</style>
