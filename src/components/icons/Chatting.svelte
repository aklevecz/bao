<div class="chat-icon">💬</div>

<style>
  .chat-icon {
    cursor: pointer;
    text-align: center;
    overflow: visible;
    font-size: 60px;
    animation: bounce infinite alternate 1s;
  }
  @keyframes bounce {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(1.5);
    }
  }
</style>
